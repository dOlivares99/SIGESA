@* @model WEB.Models.ViewModels.PagoFormViewModel
@{
    ViewData["Title"] = "Registrar pago";
    var eventos      = ViewBag.Eventos      as Microsoft.AspNetCore.Mvc.Rendering.SelectList;
    var tiposPago    = ViewBag.TiposPago    as Microsoft.AspNetCore.Mvc.Rendering.SelectList;
    var metodosPago  = ViewBag.MetodosPago  as Microsoft.AspNetCore.Mvc.Rendering.SelectList;
}

<div class="d-flex align-items-center gap-3 mb-4">
    <a asp-action="Index" class="btn btn-sm btn-outline-secondary">
        <i class="ti ti-arrow-left"></i>
    </a>
    <div>
        <h5 class="fw-bold mb-0">Registrar pago</h5>
        <small class="text-muted">Registra un pago asociado a un evento</small>
    </div>
</div>

<div class="card" style="max-width:560px;">
    <div class="card-body p-4">

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger py-2">
                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    <div><i class="ti ti-alert-circle me-1"></i>@error.ErrorMessage</div>
                }
            </div>
        }

        <form asp-action="Crear" method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label asp-for="EventoId" class="form-label fw-medium">Evento</label>
                <select asp-for="EventoId" asp-items="eventos" class="form-select">
                    <option value="">— Selecciona un evento —</option>
                </select>
                <span asp-validation-for="EventoId" class="text-danger small"></span>
            </div>

            <div class="row g-3 mb-3">
                <div class="col-6">
                    <label asp-for="TipoPagoId" class="form-label fw-medium">Tipo de pago</label>
                    <select asp-for="TipoPagoId" asp-items="tiposPago" class="form-select">
                        <option value="">— Tipo —</option>
                    </select>
                    <span asp-validation-for="TipoPagoId" class="text-danger small"></span>
                </div>
                <div class="col-6">
                    <label asp-for="MetodoPagoId" class="form-label fw-medium">Método de pago</label>
                    <select asp-for="MetodoPagoId" asp-items="metodosPago" class="form-select">
                        <option value="">— Método —</option>
                    </select>
                    <span asp-validation-for="MetodoPagoId" class="text-danger small"></span>
                </div>
            </div>

            <div class="row g-3 mb-3">
                <div class="col-7">
                    <label asp-for="Monto" class="form-label fw-medium">Monto (₡)</label>
                    <input asp-for="Monto" type="number" step="0.01" min="0.01"
                           class="form-control" placeholder="0.00" />
                    <span asp-validation-for="Monto" class="text-danger small"></span>
                </div>
                <div class="col-5">
                    <label asp-for="FechaPago" class="form-label fw-medium">Fecha</label>
                    <input asp-for="FechaPago" type="date" class="form-control" />
                    <span asp-validation-for="FechaPago" class="text-danger small"></span>
                </div>
            </div>

            <div class="mb-3">
                <label asp-for="UrlComprobante" class="form-label fw-medium">
                    URL del comprobante <span class="text-muted fw-normal">(opcional)</span>
                </label>
                <input asp-for="UrlComprobante" class="form-control"
                       placeholder="https://..." />
                <span asp-validation-for="UrlComprobante" class="text-danger small"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Observacion" class="form-label fw-medium">
                    Observación <span class="text-muted fw-normal">(opcional)</span>
                </label>
                <textarea asp-for="Observacion" class="form-control" rows="2"
                          placeholder="Notas adicionales sobre el pago..."></textarea>
                <span asp-validation-for="Observacion" class="text-danger small"></span>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary px-4">
                    <i class="ti ti-device-floppy me-1"></i> Guardar
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>

    </div>
</div> *@
