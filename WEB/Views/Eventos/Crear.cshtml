@* @model WEB.Models.ViewModels.EventoFormViewModel
@{
    ViewData["Title"] = "Nuevo evento";
    var clientes = ViewBag.Clientes as Microsoft.AspNetCore.Mvc.Rendering.SelectList;
    var paquetes = ViewBag.Paquetes as Microsoft.AspNetCore.Mvc.Rendering.SelectList;
}

<div class="d-flex align-items-center gap-3 mb-4">
    <a asp-action="Index" class="btn btn-sm btn-outline-secondary">
        <i class="ti ti-arrow-left"></i>
    </a>
    <div>
        <h5 class="fw-bold mb-0">Nuevo evento</h5>
        <small class="text-muted">Registra un nuevo evento para un cliente</small>
    </div>
</div>

<div class="card" style="max-width:560px;">
    <div class="card-body p-4">

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger py-2">
                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    <div><i class="ti ti-alert-circle me-1"></i>@error.ErrorMessage</div>
                }
            </div>
        }

        <form asp-action="Crear" method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label asp-for="ClienteId" class="form-label fw-medium">Cliente</label>
                <select asp-for="ClienteId" asp-items="clientes" class="form-select">
                    <option value="">— Selecciona un cliente —</option>
                </select>
                <span asp-validation-for="ClienteId" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="PaqueteId" class="form-label fw-medium">Paquete</label>
                <select asp-for="PaqueteId" asp-items="paquetes" class="form-select">
                    <option value="">— Selecciona un paquete —</option>
                </select>
                <span asp-validation-for="PaqueteId" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="TipoEvento" class="form-label fw-medium">Tipo de evento</label>
                <input asp-for="TipoEvento" class="form-control"
                       placeholder="Ej: Boda, Quinceañera, Cumpleaños..." />
                <span asp-validation-for="TipoEvento" class="text-danger small"></span>
            </div>

            <div class="row g-3 mb-3">
                <div class="col-7">
                    <label asp-for="FechaEvento" class="form-label fw-medium">Fecha del evento</label>
                    <input asp-for="FechaEvento" type="date" class="form-control" />
                    <span asp-validation-for="FechaEvento" class="text-danger small"></span>
                </div>
                <div class="col-5">
                    <label asp-for="NumPersonas" class="form-label fw-medium">N.° personas</label>
                    <input asp-for="NumPersonas" type="number" min="1" class="form-control" />
                    <span asp-validation-for="NumPersonas" class="text-danger small"></span>
                </div>
            </div>

            <div class="mb-4">
                <label asp-for="Notas" class="form-label fw-medium">
                    Notas <span class="text-muted fw-normal">(opcional)</span>
                </label>
                <textarea asp-for="Notas" class="form-control" rows="3"
                          placeholder="Indicaciones especiales, detalles adicionales..."></textarea>
                <span asp-validation-for="Notas" class="text-danger small"></span>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary px-4">
                    <i class="ti ti-device-floppy me-1"></i> Guardar
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>

    </div>
</div> *@
